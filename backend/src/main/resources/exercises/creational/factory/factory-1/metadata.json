{
  "id": "factory-1",
  "title": "Centralise shape creation in a factory",
  "difficulty": "Easy",
  "description": "ShapeFactory receives a type string and dimensions, and is expected to return the matching Shape \u2014 but the implementation is missing. The concrete shape classes (Circle, Rectangle, Triangle) already exist.",
  "editableFiles": [
    "ShapeFactory.java"
  ],
  "hints": [
    "Create a class called ShapeFactory with a static create(String type, double... args) method",
    "Use a switch statement on the type string to decide which shape to instantiate",
    "Throw an IllegalArgumentException for unknown types"
  ],
  "issues": [
    "Every caller that needs a shape contains its own if/else block",
    "Adding a new shape type means updating every call site"
  ],
  "overallGoal": "Use the Factory Method pattern to centralise all shape-creation logic inside ShapeFactory, removing the need for callers to know which concrete class to instantiate.",
  "goals": [
    "Create a ShapeFactory class with a static create(String type) method",
    "Move all shape-instantiation branching logic into create()",
    "Return the correct Shape subclass for each type string, and throw for unknown types"
  ]
}
