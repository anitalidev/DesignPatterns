{
  "id": "factory-2",
  "title": "Register creators in a factory map",
  "difficulty": "Medium",
  "description": "VehicleFactory creates vehicles via a switch statement keyed on a type string. Each supported vehicle type is a separate case, and the factory must be edited whenever a new type is introduced.",
  "editableFiles": [
    "VehicleFactory.java"
  ],
  "hints": [
    "Store creators in a Map<String, Supplier<Vehicle>> and look them up by type",
    "Add a register(String type, Supplier<Vehicle> creator) method to allow new types",
    "Throw an IllegalArgumentException when the type is not registered"
  ],
  "issues": [
    "The factory uses a growing switch statement that must be edited for every new type",
    "Adding a vehicle type requires changing the factory itself rather than just registering one"
  ],
  "overallGoal": "Use the Factory Method pattern with a registry map so new vehicle types can be added without ever modifying VehicleFactory itself.",
  "goals": [
    "Replace the switch statement with a Map<String, Supplier<Vehicle>>",
    "Add a register(String type, Supplier<Vehicle>) method so new types can be added without touching the factory",
    "In create(), look up the type in the map and throw a clear exception for unknown types"
  ]
}
