# Behavioural Patterns

Behavioural patterns are concerned with **communication and responsibility between objects**. They define how objects interact and distribute work, making it easier to change behaviour at runtime without rewriting your class hierarchy.

Common examples include **Observer**, **Strategy**, **Command**, and **Iterator**. These patterns are especially useful when you need to decouple senders from receivers, or when behaviour needs to vary independently of the objects that use it.
