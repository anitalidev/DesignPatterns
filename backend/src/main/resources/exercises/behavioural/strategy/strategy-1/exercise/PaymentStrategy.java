// TODO: define PaymentStrategy interface with:
//   String pay(double amount)   — processes the payment and returns a confirmation message
//   double getBalance()         — returns the current remaining balance
